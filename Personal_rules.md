# 个人文库规则

本文件记录用户的 Zotero 文库整理规则。处理文献前必须先读本文件；用户更新规则时，以本文件最新内容为准。

## 分类规则

分类是 Zotero 管理文献的首要方式。只能使用下列已设定分类，不允许自创分类。Zotero 允许每篇文献有多个分类；拿不定时可以不添加分类。

当前允许分类：

- `Clinical Study`：所有涉及药物临床实验研究的文献。
- `Spatial`：空间组学相关文献的总分类。当前不直接放文献，只作为以下 `Spatial ...` 子分类的父分类。
- `Spatial analysis`：数据预处理相关方法文献，如数据降噪、细胞分割等；也包括新颖的生信分析思路、多模态数据联合分析方法等。重点是非建模的数据处理，但可能涉及模型。
- `Spatial benchmarking`：空间组学试验方法、生信方法、模型方法的 benchmarking 文章。一般模型文章附带的小规模 benchmarking 不放这里；本类主要收主旨就是 benchmarking 的文章。若 benchmarking 文章也提出方法，按 Abstract 主次判断。
- `Spatial data & finding`：一般空间组学队列文章。通常不强调方法学创新，而主要在样本、数据或生物学发现层面有新颖性。
- `Spatial modeling`：空间组学相关建模文章。与 `Spatial analysis` 的主要区别是文章目的是否明确为预测某个指标，而不是单纯数据处理。预训练、大模型文章也放在这里，即使训练本身没有直接指标预测目的，因为其目标是支持下游任务建模。
- `Spatial wet tech`：空间组学实验技术创新文章，主要在样本预处理、检测方法等实验方法学上有创新。
- `Target Discovery`：药物发现、靶点发现相关的算法或应用案例文章。

## Tag 规则

Tag 是 Zotero 的辅助文献标识系统。一篇文献可以有多个 Tag，且 Zotero 支持自由输入。

当前处理规则：不要自创 Tag。对于所有处理过的文章都加统一的 Tag："Codexed"

## Zotero 字段填写规则

每篇文献尽量填写以下字段：

- 条目类型：例如期刊文章、预印本、会议论文等；按 Zotero 支持的类型选择。
- 作者：只填第一作者和最后一个通讯作者；其他作者不用填。若无法确认通讯作者，填第一作者和最后一位作者，并备注。
- DOI：例如 `10.1038/s41586-025-08757-x`。
- 网址：论文主页面，例如 `https://www.nature.com/articles/s41586-025-08757-x`。
- 标题：论文完整标题。
- 短标题：总结文章关键信息；方法学文章优先填技术、模型、工具或平台名称。
- 出版物：杂志或会议名称，例如 `Nature`。
- 卷次：找得到就填，不强求，例如 `185`。
- 页码：找得到就填，不强求，例如 `1223-1239.e20`。
- ISSN：找得到就填，不强求，例如 `0028-0836, 1476-4687`。
- 期号：找得到就填，不强求，例如 `8064`。
- 刊名简称：例如 `Nature`。
- 其他/Extra：填写文章使用的组学技术平台类型，例如 `Data: Xenium+Flex`。

## PDF 附件规则

有 PDF 时，应把 PDF 关联为对应文献条目的子附件。用户直接提供 PDF 时，从该 PDF 抽取信息；只有链接或文献号时，优先从合法公开来源下载 PDF。

处理后的 PDF 放入 `paper_processed/files/`，并在 `paper_processed/processed.rdf` 中引用。没有 PDF 或下载失败时，不要阻塞元数据条目生成，在 `processing_notes.md` 记录原因。
